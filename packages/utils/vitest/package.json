{
  "name": "@bemedev/shared-vitest",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "module": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "pnpm run rm:lib && pnpm run rolldown",
    "rm:lib": "pnpx rimraf ./lib",
    "rolldown": "rolldown -c",
    "_ci": "pnpm run lint && pnpm run build",
    "fmt": "oxfmt",
    "lint": "oxlint --fix && pnpm run fmt",
    "pretest": "pnpm run build",
    "test": "echo \"No tests needed\"",
    "posttest": "pnpm run fmt"
  },
  "dependencies": {
    "@bemedev/dev-utils": "^1.1.0",
    "vitest": "^4.1.10"
  },
  "devDependencies": {
    "rolldown": "^1.2.0",
    "typescript": "^7.0.2"
  }
}
